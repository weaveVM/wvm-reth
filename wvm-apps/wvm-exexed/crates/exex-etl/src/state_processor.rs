pub struct StateProcessor {}

impl Default for StateProcessor {
    fn default() -> Self {
        Self::new()
    }
}

impl StateProcessor {
    pub fn new() -> StateProcessor {
        StateProcessor {}
    }
}
